# completion settings for the break built-in
# written by magicant
# vim: set ft=sh ts=8 sts=8 sw=8 noet:

complete -C "$1" -X -F comp/
complete -C "$1" -O i -O --iteration \
	-D "break iterative execution"
complete -C "$1" -O --help
